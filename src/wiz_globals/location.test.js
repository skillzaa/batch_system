import x  from "./location.js";
// import jest_test from "./location.js";
// import square from 'test.mjs';
 
describe('location at 10', () => {
    it('Compute the square of a number', () => {
        expect(x(10)).toEqual(120);
    });
});
describe('location at 20', () => {
    it('Compute the square of a number', () => {
        expect(x(20)).toEqual(240);
    });
});
describe('location at 30', () => {
    it('Compute the square of a number', () => {
        expect(x(30)).toEqual(360);
    });
});
describe('location at 40', () => {
    it('Compute the square of a number', () => {
        expect(x(40)).toEqual(480);
    });
});
describe('location at 50', () => {
    it('Compute the square of a number', () => {
        expect(x(50)).toEqual(600);
    });
});
describe('location at 60', () => {
    it('Compute the square of a number', () => {
        expect(x(60)).toEqual(720);
    });
});
describe('location at 70', () => {
    it('Compute the square of a number', () => {
        expect(x(70)).toEqual(840);
    });
});
describe('location at 80', () => {
    it('Compute the square of a number', () => {
        expect(x(80)).toEqual(960);
    });
});
describe('location at 90', () => {
    it('Compute the square of a number', () => {
        expect(x(90)).toEqual(1080);
    });
});
describe('location at 100', () => {
    it('Compute the square of a number', () => {
        expect(x(100)).toEqual(1200);
    });
});
