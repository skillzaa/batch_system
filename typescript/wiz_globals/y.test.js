import y  from "./y.js";
 //--app_width should be 1200 in glbl.js
describe('location at 10', () => {
    it('Compute the square of a number', () => {
        expect(y(10)).toEqual(80);
    });
});
describe('location at 20', () => {
    it('Compute the square of a number', () => {
        expect(y(20)).toEqual(160);
    });
});
describe('location at 30', () => {
    it('Compute the square of a number', () => {
        expect(y(30)).toEqual(240);
    });
});
describe('location at 40', () => {
    it('Compute the square of a number', () => {
        expect(y(40)).toEqual(320);
    });
});
describe('location at 50', () => {
    it('Compute the square of a number', () => {
        expect(y(50)).toEqual(400);
    });
});
describe('location at 60', () => {
    it('Compute the square of a number', () => {
        expect(y(60)).toEqual(480);
    });
});
describe('location at 70', () => {
    it('Compute the square of a number', () => {
        expect(y(70)).toEqual(560);
    });
});
describe('location at 80', () => {
    it('Compute the square of a number', () => {
        expect(y(80)).toEqual(640);
    });
});
describe('location at 90', () => {
    it('Compute the square of a number', () => {
        expect(y(90)).toEqual(720);
    });
});
describe('location at 100', () => {
    it('Compute the square of a number', () => {
        expect(y(100)).toEqual(800);
    });
});
